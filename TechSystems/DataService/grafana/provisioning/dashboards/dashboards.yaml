apiVersion: 1

providers:
  - name: "dataservice"
    orgId: 1
    folder: "DataService"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
